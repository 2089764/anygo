#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webot-cli/bin/webot" "$@"
  ret=$?
else 
  node  "$basedir/../webot-cli/bin/webot" "$@"
  ret=$?
fi
exit $ret
